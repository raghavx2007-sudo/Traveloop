/**
 * seedActivities — intentionally does nothing.
 * The activity catalog starts empty so users can add their own activities
 * via the admin/activity management endpoints.
 * 
 * To pre-populate with sample data during development, uncomment the block below
 * and run the server once, then comment it out again.
 */
export const seedActivities = async () => {
  // No auto-seeding — catalog is user-managed
};
